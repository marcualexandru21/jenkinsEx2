def buildApp() {
    echo "building the app..."
}

def buildApp() {
    echo "testing the app..."
}

def buildApp() {
    echo "deploying the app..."
    echo "deploying version ${VERSION}"
}